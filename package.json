{
  "name": "express-mongodb-app",
  "version": "1.0.0",
  "description": "Express application with MongoDB and Mongoose integration",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.0.0"
  }
}